#ifndef NUBE_H
#define NUBE_H
#include <Objeto.h>

class Nube:public Objeto
{
    public:
        Nube(float, int);
        Nube();
        virtual ~Nube();
    protected:
    private:
};

#endif // NUBE_H
